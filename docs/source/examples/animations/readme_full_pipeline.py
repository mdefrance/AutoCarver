"""README hero animation — the full pipeline (Discretizers + Carvers) on one
canvas: feature strip on top, ranked-groupings table below."""

NAME = "readme_full_pipeline"
FEATURE = "readme"
TARGET = "binary"
STOP_AFTER_STAGE = 4
